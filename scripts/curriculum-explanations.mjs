const reactDefinitions = [
  "React는 화면을 컴포넌트라는 독립적인 UI 단위로 나누고, 데이터가 바뀌면 필요한 화면을 선언적으로 다시 계산하는 라이브러리입니다. 컴포넌트는 보통 JSX를 반환하는 함수이며 재사용과 테스트의 기본 단위가 됩니다.",
  "JSX는 JavaScript 안에서 HTML과 비슷한 모양으로 UI 구조를 표현하는 문법 확장입니다. 브라우저가 직접 실행하는 HTML이 아니라 빌드 과정에서 React 엘리먼트를 만드는 JavaScript로 변환됩니다.",
  "Props는 부모 컴포넌트가 자식 컴포넌트에 전달하는 읽기 전용 입력값입니다. 같은 컴포넌트도 서로 다른 props를 받아 다른 내용을 렌더링할 수 있으며, 자식이 props를 직접 변경해서는 안 됩니다.",
  "React 이벤트 처리는 onClick 같은 속성에 함수를 전달해 사용자 입력에 반응하는 방식입니다. 함수를 호출한 결과가 아니라 함수 자체를 전달해야 하며, 이벤트 안에서 상태를 바꾸면 React가 화면을 다시 렌더링합니다.",
  "조건부 렌더링은 상태나 props에 따라 서로 다른 JSX를 반환하는 기법입니다. if, 삼항 연산자, 논리 AND를 사용할 수 있으며 화면에 없어야 할 요소는 null을 반환해 제외합니다.",
  "목록 렌더링은 배열을 map으로 React 엘리먼트 배열로 변환하는 방식입니다. key는 각 항목의 안정적인 신원을 알려 주어 React가 추가·삭제·재정렬된 항목을 정확히 구별하게 합니다.",
  "컴포넌트 합성은 작은 컴포넌트를 props와 children으로 조립해 더 큰 UI를 만드는 설계 방식입니다. 상속보다 합성을 사용하면 화면 구조와 동작을 느슨하게 결합하고 재사용하기 쉽습니다.",
  "스타일 적용은 className, 인라인 style 또는 CSS 모듈 등으로 React 엘리먼트에 시각 규칙을 연결하는 작업입니다. 인라인 style은 JavaScript 객체와 camelCase 속성명을 사용합니다.",
  "React 개발자 도구는 컴포넌트 트리, props, state와 렌더링 원인을 브라우저에서 조사하는 디버깅 도구입니다. 데이터가 어느 컴포넌트에서 바뀌는지 추적할 때 사용합니다.",
  "기초 UI 구현은 컴포넌트, props, 이벤트, 조건과 목록 렌더링을 결합해 데이터에서 화면을 만드는 과정입니다. UI를 작은 책임 단위로 나누고 데이터 흐름은 부모에서 자식 방향으로 유지하는 것이 핵심입니다.",
  "useState는 함수 컴포넌트가 렌더링 사이에 값을 기억하게 하는 Hook입니다. setter에 새 값을 전달하면 React가 다음 렌더링을 예약하며, 이전 값에 의존할 때는 함수형 업데이트를 사용합니다.",
  "상태 업데이트는 현재 UI의 원본 데이터를 새 값으로 교체해 재렌더링을 요청하는 작업입니다. 객체와 배열은 직접 변경하지 않고 새 객체를 만들어야 React가 변경을 안정적으로 감지할 수 있습니다.",
  "폼 상태 관리는 input의 value를 React 상태와 연결하고 onChange로 갱신하는 제어 컴포넌트 방식입니다. 화면 입력값의 단일 원천이 상태가 되어 검증과 제출 처리가 예측 가능해집니다.",
  "상태 끌어올리기는 여러 자식이 같은 데이터를 사용해야 할 때 가장 가까운 공통 부모로 상태를 이동하는 패턴입니다. 부모가 값과 변경 함수를 내려주므로 형제 컴포넌트의 데이터가 동기화됩니다.",
  "파생 상태는 기존 props나 state로 계산할 수 있는 값입니다. 별도 state로 중복 저장하면 값이 어긋날 수 있으므로 렌더링 중 계산하고, 계산 비용이 클 때만 메모이제이션을 고려합니다.",
  "useEffect는 렌더링 결과를 외부 시스템과 동기화하는 Hook입니다. 네트워크, 타이머, DOM 구독처럼 React 밖의 부수 효과에 사용하며 의존 값이 바뀐 뒤 실행됩니다.",
  "Effect 정리는 구독 해제, 타이머 제거, 요청 취소처럼 이전 Effect가 만든 외부 작업을 되돌리는 과정입니다. Effect가 반환한 함수는 다음 실행 전과 컴포넌트가 사라질 때 호출됩니다.",
  "데이터 가져오기는 서버 요청의 시작·성공·실패를 비동기로 처리해 UI 상태로 반영하는 작업입니다. Effect를 직접 쓸 때는 오래된 응답과 취소를 처리해야 하며 서버 상태 라이브러리를 쓰면 캐시를 함께 관리할 수 있습니다.",
  "로딩과 오류 UI는 비동기 작업의 대기·실패·성공 상태를 각각 명시적으로 렌더링하는 방식입니다. 사용자가 현재 상태와 재시도 방법을 알 수 있게 하며 빈 데이터 상태도 별도로 구분해야 합니다.",
  "할 일 목록은 입력, 목록 상태, 추가·완료·삭제 이벤트를 결합하는 CRUD UI입니다. 각 항목에는 안정적인 id를 사용하고 배열은 map과 filter로 불변 업데이트합니다.",
  "useRef는 렌더링을 일으키지 않으면서 값을 유지하거나 DOM 노드를 참조하는 Hook입니다. 입력 포커스, 타이머 ID처럼 화면 출력에 직접 필요하지 않은 가변 값을 보관할 때 적합합니다.",
  "Context API는 트리 깊숙한 컴포넌트에 값을 반복적으로 props로 전달하지 않고 공유하는 기능입니다. Provider의 value가 바뀌면 이를 읽는 소비자가 다시 렌더링되므로 자주 변하는 큰 값은 분리해야 합니다.",
  "useReducer는 상태 변경 규칙을 reducer 함수에 모으고 action으로 변경 의도를 전달하는 Hook입니다. 여러 필드와 전이가 얽힌 상태를 일관되게 관리하고 테스트하기 좋습니다.",
  "커스텀 Hook은 여러 컴포넌트가 공유하는 상태 로직과 Effect를 use로 시작하는 함수로 추출한 것입니다. 상태 자체를 공유하는 것이 아니라 Hook을 호출할 때마다 독립적인 상태 로직이 생성됩니다.",
  "Hook 규칙은 Hook을 React 함수의 최상위에서 같은 순서로 호출해야 한다는 제약입니다. 조건문이나 반복문 안에서 호출하면 React가 각 상태 슬롯을 이전 렌더링과 연결할 수 없습니다.",
  "컴포넌트 생명주기는 마운트, 업데이트, 언마운트 동안 렌더링과 Effect 설정·정리가 일어나는 흐름입니다. 함수 컴포넌트에서는 특정 시점보다 각 Effect가 어떤 외부 동기화를 책임지는지 중심으로 설계합니다.",
  "제어 컴포넌트는 값이 props로 결정되고 변경을 콜백으로 요청하며, 비제어 컴포넌트는 DOM이 값을 보관하고 ref로 읽습니다. 검증과 연동이 필요하면 제어 방식을, 단순 입력이나 파일 입력에는 비제어 방식을 고려합니다.",
  "Portal은 자식 컴포넌트를 논리적인 React 트리는 유지한 채 다른 DOM 위치에 렌더링하는 기능입니다. 모달과 툴팁을 overflow나 stacking context 밖에 표시할 때 사용하며 이벤트는 React 트리를 따라 전파됩니다.",
  "Error Boundary는 하위 렌더링 과정의 JavaScript 오류를 잡아 대체 UI를 보여 주는 경계입니다. 이벤트 핸들러나 임의의 비동기 오류는 잡지 않으며, 문제가 전체 화면으로 확산되는 것을 막는 데 사용합니다.",
  "상태 설계는 무엇을 state로 저장하고 어디에서 소유할지 결정하는 작업입니다. 최소한의 원본 상태만 저장하고 파생 값은 계산하며, 상태는 그것을 사용하는 컴포넌트들의 가장 가까운 공통 부모에 둡니다.",
  "React Router는 URL을 화면 상태와 연결해 경로별 컴포넌트를 렌더링하는 클라이언트 라우팅 도구입니다. 링크 이동은 전체 문서를 다시 받지 않고 현재 앱에서 일치하는 라우트를 바꿉니다.",
  "중첩 레이아웃은 부모 경로의 공통 UI 안에 자식 경로 화면을 배치하는 구조입니다. 헤더나 사이드바를 유지하면서 Outlet 위치의 콘텐츠만 경로에 따라 교체할 수 있습니다.",
  "URL 상태는 검색어, 필터, 페이지 번호처럼 공유하거나 새로고침 후 복원할 값을 pathname과 search params에 저장하는 방식입니다. 브라우저의 뒤로 가기와 링크 공유가 자연스럽게 동작합니다.",
  "인증 화면 구성은 로그인 여부와 권한에 따라 접근 가능한 라우트와 UI를 구분하는 작업입니다. 클라이언트 가드는 사용자 경험을 돕지만 보안 경계는 아니므로 서버에서도 세션과 권한을 반드시 검증해야 합니다.",
  "접근성은 키보드, 스크린 리더 등 다양한 방식으로 UI를 사용할 수 있게 만드는 품질입니다. 의미 있는 HTML, label, 포커스 순서와 충분한 대비를 우선하고 ARIA는 네이티브 의미가 부족할 때 보완합니다.",
  "TanStack Query는 원격 서버 데이터를 요청하고 캐시·재검증·로딩·오류 상태를 관리하는 라이브러리입니다. 서버 상태를 로컬 UI 상태와 분리해 중복 요청과 직접 작성할 비동기 코드를 줄입니다.",
  "서버 상태 캐싱은 queryKey로 요청 결과를 식별해 재사용하고 일정 조건에서 최신 데이터로 갱신하는 기능입니다. staleTime은 언제 낡은 것으로 볼지, gcTime은 사용하지 않는 캐시를 얼마나 보관할지 정합니다.",
  "Mutation은 생성·수정·삭제처럼 서버 데이터를 변경하는 비동기 작업입니다. 성공 후 관련 query를 무효화하거나 캐시를 직접 갱신하고, 실패·재시도·사용자 피드백을 함께 처리합니다.",
  "낙관적 업데이트는 서버 응답 전에 성공을 예상해 캐시와 UI를 먼저 바꾸는 기법입니다. 실패하면 이전 캐시로 롤백해야 하며, 충돌 가능성과 복구 비용이 낮은 상호작용에 적합합니다.",
  "게시판 구현은 목록 query, 상세 query와 작성·수정·삭제 mutation을 조합하는 서버 상태 예제입니다. 데이터별 queryKey를 일관되게 설계해야 변경 후 필요한 화면만 정확히 갱신할 수 있습니다.",
  "Zustand는 store 함수와 Hook으로 전역 클라이언트 상태를 관리하는 작은 상태 관리 라이브러리입니다. 컴포넌트는 selector로 필요한 조각만 구독하며 action을 통해 상태를 변경합니다.",
  "전역 상태 분리는 여러 화면이 공유하는 클라이언트 상태만 store에 두고, 지역 UI 상태와 서버 캐시는 각각 컴포넌트와 TanStack Query에 두는 원칙입니다. 역할을 나누면 동기화 문제를 줄일 수 있습니다.",
  "상태 영속화는 localStorage 같은 저장소에 store 일부를 기록하고 앱 시작 때 복원하는 기능입니다. 개인정보와 버전 호환성을 고려하고 서버 렌더링 환경에서는 hydration 시점 차이를 처리해야 합니다.",
  "Selector 최적화는 store 전체가 아니라 필요한 값만 선택해 해당 값이 바뀔 때만 렌더링하는 방법입니다. 매번 새 객체를 반환하는 selector는 불필요한 갱신을 만들 수 있어 얕은 비교나 원시 값 선택을 사용합니다.",
  "복합 앱 설계는 지역 상태, 전역 클라이언트 상태, URL 상태와 서버 상태를 성격에 맞는 도구로 분리하는 작업입니다. 동일 데이터를 여러 저장소에 중복 보관하지 않는 것이 일관성의 핵심입니다.",
  "React Testing Library는 구현 세부사항보다 사용자가 화면을 찾고 조작하는 방식으로 컴포넌트를 테스트하는 도구입니다. 역할과 접근 가능한 이름으로 요소를 찾으면 리팩터링에 강한 테스트가 됩니다.",
  "컴포넌트 테스트는 주어진 props와 사용자 행동에 따라 화면과 콜백이 올바르게 변하는지 검증합니다. 내부 state나 함수 대신 DOM에 드러나는 결과를 단언합니다.",
  "Hook 테스트는 커스텀 Hook의 상태 전이와 외부 효과를 격리해 검증하는 작업입니다. Hook은 React 렌더링 문맥이 필요하므로 전용 렌더 도구를 사용하고 상태 변경은 act 안에서 수행합니다.",
  "Mock Service Worker는 실제 네트워크 계층에서 요청을 가로채 정해진 응답을 돌려주는 API 모킹 도구입니다. 앱 코드를 바꾸지 않고 성공·지연·오류 응답을 테스트할 수 있습니다.",
  "테스트 전략은 단위·통합·종단 테스트의 범위를 위험도에 맞게 배치하는 계획입니다. 사용자 핵심 흐름은 통합 테스트로, 순수 계산은 단위 테스트로 검증하고 구현 세부사항에 과도하게 결합하지 않습니다.",
  "React.memo는 props가 이전과 같을 때 함수 컴포넌트의 재렌더링을 건너뛰도록 하는 최적화 도구입니다. 비교 비용도 발생하므로 실제 병목을 측정한 뒤 순수하고 자주 렌더링되는 컴포넌트에 적용합니다.",
  "useMemo는 계산 결과를, useCallback은 함수 참조를 의존성 사이에서 재사용합니다. 참조 안정성이 memo된 자식이나 다른 Hook의 의존성에 실제로 필요할 때 사용하며 의미적 정확성을 위한 도구는 아닙니다.",
  "코드 분할은 앱 코드를 여러 번들로 나누어 필요한 시점에 동적으로 불러오는 최적화입니다. React.lazy와 dynamic import를 사용하고 로딩 중에는 Suspense fallback을 보여 초기 다운로드 크기를 줄입니다.",
  "Suspense는 자식이 코드나 데이터 같은 비동기 자원을 준비하는 동안 가장 가까운 경계의 fallback UI를 대신 보여 주고, 준비되면 원래 UI로 전환하는 기능입니다. 요청을 직접 실행하는 API가 아니라 Suspense를 지원하는 lazy 컴포넌트·프레임워크·데이터 소스와 함께 로딩 경계를 선언하는 역할을 합니다.",
  "렌더링 성능 분석은 React DevTools Profiler로 어떤 컴포넌트가 왜, 얼마나 오래 렌더링됐는지 측정하는 과정입니다. 측정 결과를 바탕으로 상태 위치 조정, 컴포넌트 분리와 메모이제이션을 선택합니다.",
  "Next.js App Router는 app 디렉터리의 폴더와 파일 규칙으로 라우트, 레이아웃, 로딩과 오류 경계를 구성하는 라우팅 시스템입니다. React Server Components를 기본으로 사용해 서버와 클라이언트 작업을 분리합니다.",
  "서버 컴포넌트는 서버에서 실행되어 데이터 접근과 렌더링 결과를 전달하고, 클라이언트 컴포넌트는 브라우저에서 상태·이벤트·브라우저 API를 사용합니다. 필요한 상호작용 경계에만 'use client'를 두면 전송할 JavaScript를 줄일 수 있습니다.",
  "Server Actions는 서버에서 실행할 비동기 함수를 폼이나 클라이언트 상호작용에서 호출하는 Next.js 기능입니다. 서버 데이터 변경과 재검증을 가까이 둘 수 있지만 입력 검증·인증·권한 검사는 일반 API와 동일하게 필요합니다.",
  "배포와 모니터링은 빌드 결과를 운영 환경에 제공하고 오류, 로그, 성능 지표를 지속적으로 관찰하는 과정입니다. 환경 변수와 소스맵을 안전하게 관리하고 실제 사용자 지표로 회귀를 탐지합니다.",
  "최종 프로젝트는 컴포넌트 설계, 라우팅, 상태, 서버 데이터, 테스트와 성능을 하나의 사용자 흐름으로 통합하는 단계입니다. 기능별 경계를 명확히 하고 로딩·오류·빈 상태까지 완성해야 운영 가능한 React 앱이 됩니다.",
];

const stageRoles = {
  python: [
    "파이썬 코드의 실행과 기본값 표현",
    "컬렉션에 데이터를 저장하고 변환하는 방법",
    "함수로 로직을 분리하고 재사용하는 방법",
    "표준 기능과 외부 자원을 안전하게 다루는 방법",
    "객체 모델로 책임과 데이터를 구성하는 방법",
    "자료구조와 알고리즘으로 문제를 효율적으로 해결하는 방법",
  ],
  javascript: [
    "자바스크립트 값과 실행 흐름을 표현하는 방법",
    "배열과 객체 데이터를 불변하게 변환하는 방법",
    "함수·클로저·모듈로 동작을 구조화하는 방법",
    "브라우저 및 비동기 작업을 제어하는 방법",
    "언어의 고급 실행 원리와 품질을 다루는 방법",
    "자료구조·설계·서버 연동을 통합하는 방법",
  ],
  java: [
    "JVM에서 동작하는 정적 타입 코드의 기본 문법",
    "메서드와 기본 데이터 구조로 로직을 구성하는 방법",
    "클래스·상속·다형성으로 객체의 책임을 설계하는 방법",
    "컬렉션과 함수형 API로 데이터를 안전하게 처리하는 방법",
    "입출력·테스트·동시성을 운영 수준으로 다루는 방법",
    "알고리즘과 설계 원칙을 서비스에 통합하는 방법",
  ],
  kotlin: [
    "간결한 문법과 null 안전성으로 값을 표현하는 방법",
    "컬렉션과 고차 함수로 데이터를 변환하는 방법",
    "클래스와 함수 확장으로 도메인을 모델링하는 방법",
    "스코프 함수와 안전한 자원 처리를 적용하는 방법",
    "코루틴과 Flow로 비동기 흐름을 구조화하는 방법",
    "테스트·DSL·Android 구성 요소를 통합하는 방법",
  ],
};

export function makeDetailedExplanation(
  language,
  index,
  title,
  stage,
  solutionCode,
) {
  if (language === "react") return reactDefinitions[index];

  const role = stageRoles[language]?.[stage - 1];
  if (!role) return null;
  const firstLine = solutionCode
    .split("\n")
    .find(
      (line) =>
        line.trim() &&
        !line.trim().startsWith("//") &&
        !line.trim().startsWith("#"),
    )
    ?.trim();
  return `${title}은(는) ${role}을 다루는 핵심 개념입니다. 이 주제의 역할은 프로그램이 데이터를 받아 처리하고 다음 단계로 전달하는 규칙을 명확하게 만드는 것입니다. 예시 코드의 \`${firstLine ?? title}\` 부분에서 핵심 문법이 실제 값에 적용되는 과정을 확인할 수 있습니다. 코드를 실행한 뒤 입력값과 경계 조건을 바꾸어 출력 및 상태 변화가 어떻게 달라지는지 비교하세요.`;
}

if (reactDefinitions.length !== 60)
  throw new Error(
    `React 개념 설명은 60개여야 하지만 ${reactDefinitions.length}개입니다.`,
  );
